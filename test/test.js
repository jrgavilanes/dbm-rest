var assert = require("assert");

describe("DbmRest", function() {
    
    it ('should return true because 2 equals to 2', function() {
        
        assert.equal(2, 2);
        
    });
    
    
});